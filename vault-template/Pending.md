# Pending

<!--
The agent's PASSIVE open-loop tracker: things it is watching for the owner — a
package in transit, a reply someone owes, a refund processing. Nothing here is
work: anything the OWNER must do belongs in `Lists/Todos.md`; anything the AGENT
must execute belongs in `Tasks.md`. The agent adds a one-line item the moment a
loop appears and clears it the moment it resolves; when a date is involved, put
it in the line. Only the `## Open` section below is loaded every turn, so keep
each line short and prune aggressively — a stale loop is worse than none.
-->

## Open
