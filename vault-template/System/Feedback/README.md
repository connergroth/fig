# Feedback

Corrections the owner gives the agent land here as dated records — what was
wrong, what the owner said, what changed — so they compound instead of being
re-learned. This folder is NOT auto-loaded: a correction meant to change how the
agent ACTS must also reach `SOUL.md` (as a generalized rule, never the incident
backstory — the story stays here, the rule goes there).
