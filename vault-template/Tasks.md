# Tasks

<!--
The AGENT's assignment queue: discrete jobs the owner hands it to go DO on its
own cycles, then report back on. Distinct from the other two lists —
`Lists/Todos.md` is the owner's own work, `Pending.md` is passive watching with
nothing to do. The owner adds a task (one line plus any constraints under it);
the agent picks it up, does the real work, then moves the line to `## Done` with
a one-line outcome and reports the result. Only `## Open` is injected every
turn, so it is expensive: keep it to what's genuinely live, and clear old Done
items periodically.
-->

## Open

## Done
